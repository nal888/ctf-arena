"""arena — a self-driving CTF solver.

Point it at a CTFd competition; it pulls challenges, turns AI solvers loose on each,
verifies, and submits. Built to run on a Claude Code subscription (claude -p), no API keys.
"""
__version__ = "0.1.0"
